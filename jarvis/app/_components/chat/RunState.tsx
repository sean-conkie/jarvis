import Spinner from "../Spinner";

const Running = () => {
  return (
    <div className="flex flex-row">
      <Spinner className="text-primary" spinner="dots" />
    </div>
  );
};

export default Running;
